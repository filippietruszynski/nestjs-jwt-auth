#!/bin/bash

SERVICE_NAME="app"
CONTAINER_ID=$(docker-compose ps -q $SERVICE_NAME)

if [ -z "$CONTAINER_ID" ]; then
    echo "Error: Couldn't find a running container for service $SERVICE_NAME"
    exit 1
fi

docker cp "$CONTAINER_ID:/app/node_modules" .
echo "node_modules copied successfully!"
